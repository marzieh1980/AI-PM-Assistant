# Example environment variables for AI PM Assistant
# To run without creating a local SQLite DB file, set:
# AIPM_PERSISTENCE=false

# Default (create local DB file):
AIPM_PERSISTENCE=true

# LM Studio local server URL (if different):
# LM_STUDIO_URL=http://localhost:1234
